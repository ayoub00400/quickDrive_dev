import 'package:flutter/material.dart';

import '../../../main.dart';

// Color primaryColor = Color(0xFF417CFF);
// Color polyLineColor = Color.fromARGB(255, 40, 122, 198);
// Color borderColor = Color(0xFFEAEAEA);
// const scaffoldSecondaryDark = Color(0xFF1E1E1E);
// const scaffoldColorDark = Color(0xFF090909);
// const scaffoldColorLight = Colors.white;
// const appButtonColorDark = Color(0xFF282828);
// const dividerColor = Color(0xFFD3D3D3);
// const textPrimaryColor = Color(0xFF2E3033);
// const textSecondaryColor = Color(0xFF757575);
// const viewLineColor = Color(0xFFEAEAEA);
Color polyLineColor = Color.fromARGB(255, 40, 122, 198);
Color primaryColor = Color(0xFF2563EB); // A clean, vibrant modern blue.
Color borderColor =
    Color(0xFFDEE2E6); // A light neutral gray for clean borders.
const scaffoldSecondaryDark =
    Color(0xFF181A1B); // A cool, dark gray for a contemporary dark mode.
const scaffoldColorDark =
    Color(0xFF101214); // A near-black with a modern, muted tone.
const scaffoldColorLight =
    Color(0xFFF5F5F5); // A soft, warm white for a welcoming light mode.
const appButtonColorDark =
    Color(0xFF303436); // A deep neutral for dark mode buttons.
const dividerColor =
    Color(0xFFE0E0E0); // Subtle light gray dividers for clean separation.
const textPrimaryColor =
    Color(0xFF1F2937); // A strong dark gray for high-contrast text.
const textSecondaryColor =
    Color(0xFF6B7280); // A cool gray for muted secondary text.
const viewLineColor =
    Color(0xFFE5E7EB); // A very light gray for decorative lines.

// Color appBarBackgroundColorGlobal = Colors.white;
// Color appButtonBackgroundColorGlobal = primaryColor;
// Color defaultAppButtonTextColorGlobal = textPrimaryColorGlobal;
// const appButtonTextStyleColor = Colors.white;
// const appTextPrimaryColorWhite = Colors.white;

Color appBarBackgroundColorGlobal =
    Color(0xFFF9FAFB); // A soft neutral white with a touch of warmth.
Color appButtonBackgroundColorGlobal =
    Color(0xFF1D4ED8); // A bold and vibrant deep blue for modern buttons.
Color defaultAppButtonTextColorGlobal =
    Color(0xFFFFFFFF); // White text to maintain contrast on vibrant buttons.
const appButtonTextStyleColor =
    Color(0xFFFFFFFF); // Consistent white text for buttons.
const appTextPrimaryColorWhite =
    Color(0xFFF9FAFB); // Soft white for better light mode readability.
